function MoveResponse(addedBalls, nextColors, tiles, score, canContinue) {
	this.addedBalls = addedBalls;
	this.nextColors = nextColors;
	this.tiles = tiles;
	this.score = score;
	this.canContinue = canContinue;
}
